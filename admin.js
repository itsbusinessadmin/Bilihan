const A={section:'dashboard',session:null,orderFilter:'all',data:{products:[],categories:[],orders:[],settings:null}};const app=document.getElementById('app');const money=n=>`₱${Number(n||0).toLocaleString('en-PH',{minimumFractionDigits:2,maximumFractionDigits:2})}`;const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function configured(){return !!window.BILIHAN_SUPABASE_CONFIGURED}
async function isAdmin(){if(!A.session)return false;const {data,error}=await db.rpc('is_admin');return !error&&data===true}
async function init(){if(!configured()){renderSetup();return}const {data:{session}}=await db.auth.getSession();A.session=session;if(!session){renderLogin();return}if(!(await isAdmin())){await db.auth.signOut();A.session=null;renderLogin('This account is not listed as a Bilihan admin.');return}await loadAll();renderShell()}
function renderSetup(){app.innerHTML=`<div class="login-wrap"><div class="login-card"><img src="bilihan-logo.png" style="width:90px;border-radius:50%"><span class="eyebrow">Bilihan v3</span><h2>Connect Supabase</h2><p>Edit <strong>config.js</strong> once and paste your Supabase Project URL and anon public key, then reload this page.</p><p class="muted">Never paste a service_role key into the website.</p></div></div>`}
function renderLogin(msg=''){app.innerHTML=`<div class="login-wrap"><form id="loginForm" class="login-card admin-form"><img src="bilihan-logo.png" style="width:86px;border-radius:50%;margin:auto"><span class="eyebrow">Bilihan Admin</span><h2>Secure sign in</h2>${msg?`<div class="status-banner">${esc(msg)}</div>`:''}<label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" required></label><button class="primary-btn">Sign In</button></form></div>`;document.getElementById('loginForm').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));const {data,error}=await db.auth.signInWithPassword(d);if(error)return renderLogin(error.message);A.session=data.session;if(!(await isAdmin())){await db.auth.signOut();return renderLogin('This account is not listed as a Bilihan admin.')}await loadAll();renderShell()}}
async function loadAll(){const [p,c,o,s]=await Promise.all([db.from('products').select('*').order('sort_order'),db.from('categories').select('*').order('sort_order'),db.from('orders').select('*,order_items(*)').order('created_at',{ascending:false}),db.from('store_settings').select('*').eq('id',1).single()]);for(const r of [p,c,o,s])if(r.error)throw r.error;A.data={products:p.data,categories:c.data,orders:o.data,settings:s.data}}
function renderShell(){app.innerHTML=`<div class="admin-shell"><aside class="sidebar"><div class="admin-brand"><img src="bilihan-logo.png"><div><strong>Bilihan</strong><small style="display:block">ADMIN</small></div></div><nav class="side-nav">${[['dashboard','Dashboard'],['products','Products'],['categories','Categories'],['orders','Orders'],['settings','Store Settings'],['appearance','Appearance'],['security','Security']].map(([id,n])=>`<button data-s="${id}" class="${A.section===id?'active':''}">${n}</button>`).join('')}</nav></aside><main id="adminMain" class="admin-main"></main></div>`;document.querySelectorAll('.side-nav button').forEach(b=>b.onclick=()=>{A.section=b.dataset.s;renderShell()});const m=document.getElementById('adminMain');({dashboard,products,categories,orders,settings,appearance,security}[A.section]||dashboard)(m)}
function dashboard(m){const ps=A.data.products,os=A.data.orders;m.innerHTML=`<span class="eyebrow">Overview</span><h2>Dashboard</h2><div class="cards"><div class="metric"><small>Total Products</small><h2>${ps.length}</h2></div><div class="metric"><small>Available</small><h2>${ps.filter(p=>p.is_available&&p.stock>0).length}</h2></div><div class="metric"><small>Sold Out</small><h2>${ps.filter(p=>!p.is_available||p.stock<=0).length}</h2></div><div class="metric"><small>Total Orders</small><h2>${os.length}</h2></div></div><div class="panel"><h3>Live Supabase control center</h3><p>Products, categories, orders, images, stock and storefront settings now save to Supabase. Normal business changes no longer require editing GitHub code.</p></div>`}
async function uploadImage(file,bucket='product-images'){if(!file)return null;const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=`${crypto.randomUUID()}.${ext}`;const {error}=await db.storage.from(bucket).upload(path,file,{upsert:false});if(error)throw error;return db.storage.from(bucket).getPublicUrl(path).data.publicUrl}
function products(m) {   m.innerHTML = `     <div class="products-page">        <div class="products-page-header">          <div>           <span class="eyebrow">Catalog</span>           <h2>Products</h2>         </div>          <button           type="button"           class="primary-btn"           id="openAddProduct"         >           + Add Product         </button>        </div>         <div class="panel table-wrap">          <table class="table">            <thead>             <tr>               <th></th>               <th>Product</th>               <th>Price</th>               <th>Stock</th>               <th></th>             </tr>           </thead>            <tbody>              ${A.data.products.map(p => `               <tr>                  <td>                   <img                     class="thumb"                     src="${esc(p.image_url || 'bilihan-logo.png')}"                     alt="${esc(p.name)}"                   >                 </td>                  <td>                   <strong>${esc(p.name)}</strong>                   <br>                    <small>                     ${esc(                       A.data.categories.find(                         c => c.id === p.category_id                       )?.name || ''                     )}                   </small>                 </td>                  <td>                   ${money(p.price)}                 </td>                  <td>                   ${p.stock}                 </td>                  <td>                    <div class="row-actions">                      <button                       onclick="editProduct('${p.id}')"                     >                       Edit                     </button>                      <button                       onclick="moveProduct('${p.id}',-1)"                       title="Move up"                     >                       ↑                     </button>                      <button                       onclick="moveProduct('${p.id}',1)"                       title="Move down"                     >                       ↓                     </button>                      <button                       onclick="deleteProduct('${p.id}')"                     >                       Delete                     </button>                    </div>                  </td>                </tr>             `).join('')}            </tbody>          </table>        </div>      </div>   `;    document.getElementById('openAddProduct').onclick =     openAddProductModal; }   function openAddProductModal() {    const categoryOptions = A.data.categories     .map(c => `       <option value="${c.id}">         ${esc(c.name)}       </option>     `)     .join('');     const oldModal =     document.getElementById('addProductModal');    if (oldModal) {     oldModal.remove();   }     document.body.insertAdjacentHTML(     'beforeend',     `     <div       class="admin-modal-backdrop"       id="addProductModal"     >        <div         class="admin-modal"         role="dialog"         aria-modal="true"         aria-labelledby="addProductTitle"       >          <div class="admin-modal-header">            <div>             <span class="eyebrow">               Catalog             </span>              <h2 id="addProductTitle">               Add Product             </h2>           </div>            <button             type="button"             class="admin-modal-close"             id="closeAddProduct"             aria-label="Close"           >             ×           </button>          </div>           <form           id="addProductForm"           class="admin-form"         >            <label>             Product name              <input               name="name"               placeholder="Product name"               required             >           </label>             <label>             Description              <textarea               name="description"               rows="4"               placeholder="Description"               required             ></textarea>           </label>             <label>             Category              <select               name="category_id"               required             >               ${categoryOptions}             </select>           </label>             <div class="edit-product-row">              <label>               Price                <input                 name="price"                 type="number"                 min="0"                 step="0.01"                 placeholder="0.00"                 required               >             </label>               <label>               Stock                <input                 name="stock"                 type="number"                 min="0"                 step="1"                 placeholder="0"                 required               >             </label>            </div>             <label>             Product photo              <input               name="image_file"               type="file"               accept="image/*"             >           </label>             <label>             Image URL              <input               name="image_url"               placeholder="Or paste an image URL"             >           </label>             <label class="edit-available-row">              <input               type="checkbox"               name="is_available"               checked             >              <span>               <strong>Available</strong>                <small>                 Customers can order this product                 while stock is available.               </small>             </span>            </label>             <div class="admin-modal-actions">              <button               type="button"               class="modal-secondary-btn"               id="cancelAddProduct"             >               Cancel             </button>              <button               type="submit"               class="primary-btn"               id="saveAddProduct"             >               Save Product             </button>            </div>          </form>        </div>      </div>     `   );     const modal =     document.getElementById('addProductModal');    const form =     document.getElementById('addProductForm');    const closeButton =     document.getElementById('closeAddProduct');    const cancelButton =     document.getElementById('cancelAddProduct');     document.body.classList.add('modal-open');     const closeModal = () => {      modal.remove();      document.body.classList.remove(       'modal-open'     );      document.removeEventListener(       'keydown',       escapeModal     );    };     const escapeModal = e => {      if (e.key === 'Escape') {       closeModal();     }    };     closeButton.onclick = closeModal;    cancelButton.onclick = closeModal;     modal.addEventListener('click', e => {      if (e.target === modal) {       closeModal();     }    });     document.addEventListener(     'keydown',     escapeModal   );     form.onsubmit = async e => {      e.preventDefault();       const saveButton =       document.getElementById(         'saveAddProduct'       );       saveButton.disabled = true;      saveButton.textContent =       'Saving...';       const fd =       new FormData(form);       try {        const file =         fd.get('image_file');         let imageUrl =         String(           fd.get('image_url') || ''         ).trim();         if (file && file.size) {          imageUrl =           await uploadImage(file);        }         const stock =         Number(fd.get('stock'));         const row = {          name:           String(             fd.get('name') || ''           ).trim(),          description:           String(             fd.get('description') || ''           ).trim(),          category_id:           fd.get('category_id'),          price:           Number(fd.get('price')),          stock:           stock,          is_available:           fd.get('is_available') === 'on' &&           stock > 0,          image_url:           imageUrl || null,          sort_order:           A.data.products.length + 1        };         const { error } =         await db           .from('products')           .insert(row);         if (error) {         throw error;       }         await loadAll();        closeModal();        renderShell();       } catch (err) {        console.error(err);        alert(         'Unable to add product: ' +         err.message       );         saveButton.disabled = false;        saveButton.textContent =         'Save Product';      }    };  }
window.editProduct = id => {   const p = A.data.products.find(x => x.id === id);    if (!p) {     alert('Product not found.');     return;   }    const categoryOptions = A.data.categories     .map(c => `       <option         value="${c.id}"         ${c.id === p.category_id ? 'selected' : ''}       >         ${esc(c.name)}       </option>     `)     .join('');    const oldModal = document.getElementById('editProductModal');    if (oldModal) {     oldModal.remove();   }    document.body.insertAdjacentHTML(     'beforeend',     `     <div class="admin-modal-backdrop" id="editProductModal">        <div         class="admin-modal"         role="dialog"         aria-modal="true"         aria-labelledby="editProductTitle"       >          <div class="admin-modal-header">            <div>             <span class="eyebrow">Product editor</span>             <h2 id="editProductTitle">Edit Product</h2>           </div>            <button             type="button"             class="admin-modal-close"             id="closeEditProduct"             aria-label="Close"           >             ×           </button>          </div>          <form id="editProductForm" class="admin-form">            <div class="edit-product-image-area">              <img               id="editProductPreview"               src="${esc(p.image_url || 'bilihan-logo.png')}"               alt="${esc(p.name)}"             >              <div>               <strong>Product photo</strong>               <p class="muted">                 Upload a new photo or keep the current image.               </p>             </div>            </div>            <label>             Product name              <input               name="name"               value="${esc(p.name)}"               required             >           </label>            <label>             Description              <textarea               name="description"               rows="4"               required             >${esc(p.description || '')}</textarea>           </label>            <label>             Category              <select name="category_id" required>               ${categoryOptions}             </select>           </label>            <div class="edit-product-row">              <label>               Price                <input                 name="price"                 type="number"                 min="0"                 step="0.01"                 value="${Number(p.price || 0)}"                 required               >             </label>              <label>               Stock                <input                 name="stock"                 type="number"                 min="0"                 step="1"                 value="${Number(p.stock || 0)}"                 required               >             </label>            </div>            <label>             Upload new product photo              <input               name="image_file"               id="editProductFile"               type="file"               accept="image/*"             >           </label>            <label>             Image URL              <input               name="image_url"               value="${esc(p.image_url || '')}"               placeholder="Or paste an image URL"             >           </label>            <label class="edit-available-row">              <input               type="checkbox"               name="is_available"               ${p.is_available ? 'checked' : ''}             >              <span>               <strong>Available</strong>                <small>                 Customers can order this product while stock is available.               </small>             </span>            </label>            <div class="admin-modal-actions">              <button               type="button"               class="modal-secondary-btn"               id="cancelEditProduct"             >               Cancel             </button>              <button               type="submit"               class="primary-btn"               id="saveEditProduct"             >               Save Changes             </button>            </div>          </form>        </div>      </div>     `   );    const modal = document.getElementById('editProductModal');   const form = document.getElementById('editProductForm');   const closeButton = document.getElementById('closeEditProduct');   const cancelButton = document.getElementById('cancelEditProduct');   const fileInput = document.getElementById('editProductFile');   const preview = document.getElementById('editProductPreview');    document.body.classList.add('modal-open');    const closeModal = () => {     modal.remove();     document.body.classList.remove('modal-open');     document.removeEventListener('keydown', escapeModal);   };    closeButton.onclick = closeModal;   cancelButton.onclick = closeModal;    modal.addEventListener('click', e => {     if (e.target === modal) {       closeModal();     }   });    const escapeModal = e => {     if (e.key === 'Escape') {       closeModal();     }   };    document.addEventListener('keydown', escapeModal);    fileInput.onchange = () => {     const file = fileInput.files[0];      if (!file) {       return;     }      const temporaryUrl = URL.createObjectURL(file);      preview.src = temporaryUrl;   };    form.onsubmit = async e => {     e.preventDefault();      const saveButton = document.getElementById('saveEditProduct');      saveButton.disabled = true;     saveButton.textContent = 'Saving...';      const fd = new FormData(form);      try {       const file = fd.get('image_file');        let imageUrl = String(         fd.get('image_url') || ''       ).trim();        /*        * If you selected a new product photo,        * upload it to your existing Supabase        * product-images bucket.        */       if (file && file.size) {         imageUrl = await uploadImage(file);       }        /*        * If no new photo or URL was entered,        * keep the product's existing photo.        */       if (!imageUrl) {         imageUrl = p.image_url || null;       }        const stock = Number(fd.get('stock'));        const updatedProduct = {         name: String(           fd.get('name') || ''         ).trim(),          description: String(           fd.get('description') || ''         ).trim(),          category_id: fd.get('category_id'),          price: Number(fd.get('price')),          stock: stock,          image_url: imageUrl,          is_available:           fd.get('is_available') === 'on' &&           stock > 0       };        const { error } = await db         .from('products')         .update(updatedProduct)         .eq('id', id);        if (error) {         throw error;       }        await loadAll();        closeModal();        renderShell();      } catch (err) {       console.error(err);        alert(         'Unable to update product: ' +         err.message       );        saveButton.disabled = false;       saveButton.textContent = 'Save Changes';     }   }; };
window.deleteProduct=async id=>{if(!confirm('Delete this product?'))return;const {error}=await db.from('products').delete().eq('id',id);if(error)return alert(error.message);await loadAll();renderShell()};
window.moveProduct=async(id,d)=>{const s=[...A.data.products].sort((a,b)=>a.sort_order-b.sort_order),i=s.findIndex(p=>p.id===id),j=i+d;if(j<0||j>=s.length)return;const a=s[i],b=s[j];const {error:e1}=await db.from('products').update({sort_order:b.sort_order}).eq('id',a.id);const {error:e2}=await db.from('products').update({sort_order:a.sort_order}).eq('id',b.id);if(e1||e2)return alert((e1||e2).message);await loadAll();renderShell()};
function categories(m){m.innerHTML=`<span class="eyebrow">Menu structure</span><h2>Categories</h2><div class="admin-grid"><div class="panel">${A.data.categories.map(c=>`<div class="summary-row"><strong>${esc(c.name)}</strong><div class="row-actions"><button onclick="renameCategory('${c.id}')">Rename</button><button onclick="moveCategory('${c.id}',-1)">↑</button><button onclick="moveCategory('${c.id}',1)">↓</button><button onclick="deleteCategory('${c.id}')">Delete</button></div></div>`).join('')}</div><form id="catForm" class="panel admin-form"><input name="name" placeholder="New category name" required><button class="primary-btn">Add Category</button></form></div>`;document.getElementById('catForm').onsubmit=async e=>{e.preventDefault();const name=new FormData(e.currentTarget).get('name');const {error}=await db.from('categories').insert({name,sort_order:A.data.categories.length+1});if(error)return alert(error.message);await loadAll();renderShell()}}
window.renameCategory=async id=>{const c=A.data.categories.find(x=>x.id===id),n=prompt('Category name',c.name);if(!n)return;const {error}=await db.from('categories').update({name:n}).eq('id',id);if(error)return alert(error.message);await loadAll();renderShell()};window.deleteCategory=async id=>{if(A.data.products.some(p=>p.category_id===id))return alert('Move or delete products in this category first.');if(!confirm('Delete category?'))return;const {error}=await db.from('categories').delete().eq('id',id);if(error)return alert(error.message);await loadAll();renderShell()};window.moveCategory=async(id,d)=>{const s=[...A.data.categories].sort((a,b)=>a.sort_order-b.sort_order),i=s.findIndex(c=>c.id===id),j=i+d;if(j<0||j>=s.length)return;const a=s[i],b=s[j];await db.from('categories').update({sort_order:b.sort_order}).eq('id',a.id);await db.from('categories').update({sort_order:a.sort_order}).eq('id',b.id);await loadAll();renderShell()};
const PAY_COLORS={Paid:{bg:'#dcfce7',text:'#166534',border:'#86efac'},Pending:{bg:'#fef9c3',text:'#854d0e',border:'#fde047'},'Not Paid':{bg:'#fee2e2',text:'#991b1b',border:'#fca5a5'}};
function paySelectHtml(o){const st=o.payment_status||'Pending';const c=PAY_COLORS[st]||PAY_COLORS.Pending;return `<select onchange="updatePaymentStatus('${o.id}',this.value)" style="font-weight:700;padding:4px 8px;border-radius:8px;border:1px solid ${c.border};background:${c.bg};color:${c.text};cursor:pointer">${Object.keys(PAY_COLORS).map(k=>`<option value="${k}" ${k===st?'selected':''}>${k}</option>`).join('')}</select>`}
function orders(m){
  const totals=A.data.orders.reduce((acc,o)=>{const st=o.payment_status||'Pending';acc.sell+=+o.total;if(st==='Paid')acc.paid+=+o.total;else acc.unpaid+=+o.total;return acc},{sell:0,paid:0,unpaid:0});
  m.innerHTML=`<div class="summary-row" style="align-items:flex-start;flex-wrap:wrap;gap:16px"><div><span class="eyebrow">Customer orders</span><h2>Orders</h2></div><div class="cards" style="display:flex;gap:12px"><div class="metric"><small>Total Sell</small><h2>${money(totals.sell)}</h2></div><div class="metric" style="border-color:#86efac"><small>Total Paid</small><h2 style="color:#166534">${money(totals.paid)}</h2></div><div class="metric" style="border-color:#fca5a5"><small>Total Unpaid</small><h2 style="color:#991b1b">${money(totals.unpaid)}</h2></div></div></div>
  <div class="panel"><input id="orderSearch" placeholder="Search name, phone, order number" style="width:100%;padding:12px;border-radius:12px;border:1px solid var(--line);background:var(--bg);color:var(--text)">
  <div id="payFilters" style="display:flex;gap:8px;margin-top:12px;flex-wrap:wrap">${['all','Paid','Pending','Not Paid'].map(f=>{const active=A.orderFilter===f;const c=f==='all'?null:PAY_COLORS[f];const bg=active?(c?c.bg:'var(--text)'):'transparent';const text=active?(c?c.text:'var(--bg)'):'var(--text)';const border=c?c.border:'var(--line)';return `<button data-f="${f}" style="padding:6px 14px;border-radius:999px;border:1px solid ${border};background:${bg};color:${text};font-weight:600;cursor:pointer">${f==='all'?'All':f}</button>`}).join('')}</div>
  </div>
  <div id="orderList" class="panel table-wrap"></div>`;
  document.querySelectorAll('#payFilters button').forEach(b=>b.onclick=()=>{A.orderFilter=b.dataset.f;orders(m)});
  const draw=()=>{
    const t=document.getElementById('orderSearch').value.toLowerCase();
    const rows=A.data.orders.filter(o=>{
      const matchesSearch=`${o.order_code} ${o.customer_name} ${o.phone||''}`.toLowerCase().includes(t);
      const matchesFilter=A.orderFilter==='all'||((o.payment_status||'Pending')===A.orderFilter);
      return matchesSearch&&matchesFilter;
    });
    document.getElementById('orderList').innerHTML=rows.length?`<table class="table"><thead><tr><th>Order #</th><th>Payment Status</th><th>Date</th><th>Customer</th><th>Phone</th><th>Fulfillment</th><th>Address</th><th>Preferred Date</th><th>Payment</th><th>Items</th><th>Total</th><th>Status</th><th>Cancellation Reason</th><th></th></tr></thead><tbody>${rows.map(o=>`<tr><td><strong>#${esc(o.order_code)}</strong></td><td>${paySelectHtml(o)}</td><td>${new Date(o.created_at).toLocaleString()}</td><td>${esc(o.customer_name)}</td><td>${esc(o.phone||'—')}</td><td>${esc(o.fulfillment)}</td><td>${esc(o.address||'—')}</td><td>${esc(o.preferred_date)}</td><td>${esc(o.payment_method)}</td><td>${(o.order_items||[]).map(i=>`${esc(i.product_name)} × ${i.qty}`).join(', ')}</td><td>${money(o.total)}</td><td>${esc(o.status)}</td><td>${esc(o.cancellation_reason||'—')}</td><td><button onclick="deleteOrder('${o.id}')">Delete</button></td></tr>`).join('')}</tbody></table>`:'<p style="padding:20px">No orders match this filter.</p>';
  };
  document.getElementById('orderSearch').oninput=draw;
  draw();
}
window.updatePaymentStatus=async(id,status)=>{const {error}=await db.from('orders').update({payment_status:status}).eq('id',id);if(error)return alert(error.message);const o=A.data.orders.find(x=>x.id===id);if(o)o.payment_status=status;orders(document.getElementById('adminMain'))};
window.deleteOrder=async id=>{if(!confirm('Delete this order permanently?'))return;const {error}=await db.from('orders').delete().eq('id',id);if(error)return alert(error.message);await loadAll();renderShell()};
function settings(m){const s=A.data.settings;m.innerHTML=`<span class="eyebrow">Business info</span><h2>Store Settings</h2><form id="settingsForm" class="panel admin-form"><label>Business name<input name="business_name" value="${esc(s.business_name)}"></label><label>Pickup location<textarea name="pickup_location">${esc(s.pickup_location)}</textarea></label><label>QR image<input name="qr_file" type="file" accept="image/*"></label><input name="qr_image_url" value="${esc(s.qr_image_url||'')}" placeholder="Or QR image URL"><button class="primary-btn">Save Settings</button></form>`;document.getElementById('settingsForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);let qr=String(fd.get('qr_image_url')||'');const file=fd.get('qr_file');try{if(file&&file.size)qr=await uploadImage(file,'store-assets');const row={business_name:fd.get('business_name'),pickup_location:fd.get('pickup_location'),qr_image_url:qr};const {error}=await db.from('store_settings').update(row).eq('id',1);if(error)throw error;await loadAll();alert('Settings saved.');renderShell()}catch(err){alert(err.message)}}}
function appearance(m){const s=A.data.settings;const existing=s.hero_images||[];const slots=Math.max(5,existing.length);const heroSlots=Array.from({length:slots},(_,i)=>`<label>Hero image ${i+1}${existing[i]?` <img src="${esc(existing[i])}" style="height:34px;vertical-align:middle;border-radius:6px;margin-left:8px">`:''}<input name="hero_file_${i}" type="file" accept="image/*"></label>`).join('');m.innerHTML=`<span class="eyebrow">Storefront content</span><h2>Appearance</h2><form id="appearanceForm" class="panel admin-form"><label>Store Logo${s.logo_url?` <img src="${esc(s.logo_url)}" style="width:70px;height:70px;object-fit:cover;border-radius:50%;vertical-align:middle;margin-left:10px;border:1px solid var(--line)">`:` <img src="bilihan-logo.png" style="width:70px;height:70px;object-fit:cover;border-radius:50%;vertical-align:middle;margin-left:10px;border:1px solid var(--line)">`}<input name="logo_file" type="file" accept="image/*"></label><label>Hero title<input name="hero_title" value="${esc(s.hero_title||'')}"></label><label>Hero tagline<textarea name="hero_tagline">${esc(s.hero_tagline||'')}</textarea></label>${heroSlots}<label>About text<textarea name="about_text" rows="6">${esc(s.about_text||'')}</textarea></label><label>About image<input name="about_file" type="file" accept="image/*"></label><input name="about_image_url" value="${esc(s.about_image_url||'')}" placeholder="Or About image URL"><button class="primary-btn">Save Appearance</button></form>`;document.getElementById('appearanceForm').onsubmit=async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);let logo=s.logo_url||null;let about=String(fd.get('about_image_url')||'');const logoFile=fd.get('logo_file');const aboutFile=fd.get('about_file');try{if(logoFile&&logoFile.size)logo=await uploadImage(logoFile,'store-assets');if(aboutFile&&aboutFile.size)about=await uploadImage(aboutFile,'store-assets');const heroUrls=[];for(let i=0;i<slots;i++){const f=fd.get(`hero_file_${i}`);if(f&&f.size){heroUrls.push(await uploadImage(f,'store-assets'))}else if(existing[i]){heroUrls.push(existing[i])}}const row={logo_url:logo,hero_title:fd.get('hero_title'),hero_tagline:fd.get('hero_tagline'),hero_images:heroUrls,about_text:fd.get('about_text'),about_image_url:about};const {error}=await db.from('store_settings').update(row).eq('id',1);if(error)throw error;await loadAll();alert('Appearance saved.');renderShell()}catch(err){alert(err.message)}}}
function security(m){m.innerHTML=`<span class="eyebrow">Access</span><h2>Security</h2><div class="panel"><p><strong>Signed in as:</strong> ${esc(A.session.user.email)}</p><p>Admin access is protected by Supabase Auth and the <code>admin_users</code> table. Your service-role key is never exposed to the browser.</p></div><div class="panel"><button class="danger-btn" id="logout">Log Out</button></div>`;document.getElementById('logout').onclick=async()=>{await db.auth.signOut();A.session=null;renderLogin()}}
init().catch(e=>{console.error(e);app.innerHTML=`<div class="login-wrap"><div class="login-card"><h2>Bilihan Admin</h2><p>${esc(e.message)}</p></div></div>`});
