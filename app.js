const GOOGLE_SHEETS_WEB_APP_URL = 'https://script.google.com/macros/s/AKfycbwDiNU-R9HQDL79QlO6kObtnpS7XCxc2_xoHL3Dk1becwEEjPtAx43WZcUpNWDW3L35/exec';
const LS = { cart:'bilihan_cart_v3', theme:'bilihan_theme_v3', latestOrder:'bilihan_latest_order_v3', cache:'bilihan_cache_v3', pendingCancel:'bilihan_pending_cancel_v3' };
const FALLBACK = {
  settings:{business_name:'Bilihan',phone:'+63 900 000 0000',messenger_url:'https://m.me/',instagram_url:'https://instagram.com/',pickup_location:'Your pickup location here',qr_image_url:'bilihan-logo.png',hero_title:'Good food, made easy.',hero_tagline:'From everyday favorites to satisfying cravings, find something good at Bilihan.',about_text:'Bilihan is your easy online food stop for everyday favorites, cravings, meals, snacks, and more.',about_image_url:'https://images.unsplash.com/photo-1504674900247-0877df9cc836?auto=format&fit=crop&w=1400&q=80',hero_images:['https://images.unsplash.com/photo-1504674900247-0877df9cc836?auto=format&fit=crop&w=1600&q=80']},
  categories:[],products:[]
};
const state={data:null,category:'all',cart:JSON.parse(localStorage.getItem(LS.cart)||'[]'),heroIndex:0,online:true};
const $=id=>document.getElementById(id); const money=n=>`₱${Number(n||0).toLocaleString('en-PH',{minimumFractionDigits:2,maximumFractionDigits:2})}`;
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function toast(msg){$('toast').textContent=msg;$('toast').classList.remove('hidden');setTimeout(()=>$('toast').classList.add('hidden'),1800)}
function saveCart(){localStorage.setItem(LS.cart,JSON.stringify(state.cart));renderCart()}
async function bootstrap(){
  if(!window.BILIHAN_SUPABASE_CONFIGURED){state.online=false; state.data=JSON.parse(localStorage.getItem(LS.cache)||'null')||structuredClone(FALLBACK);renderAll();return;}
  renderSkeletons();
  try{
    const [{data:categories,error:ce},{data:products,error:pe},{data:settings,error:se}] = await Promise.all([
      db.from('categories').select('*').order('sort_order'),
      db.from('products').select('*').order('sort_order'),
      db.from('store_settings').select('*').eq('id',1).single()
    ]);
    if(ce||pe||se) throw ce||pe||se;
    state.data={categories,products,settings};state.online=true;localStorage.setItem(LS.cache,JSON.stringify(state.data));
  }catch(e){console.error(e);state.online=false;state.data=JSON.parse(localStorage.getItem(LS.cache)||'null')||structuredClone(FALLBACK)}
  renderAll();retryPendingCancel();
}
function renderSkeletons(){$('menuGrid').innerHTML=Array.from({length:8},()=>'<div class="skeleton"></div>').join('')}
function renderAll(){renderSettings();renderCategories();renderProducts();renderCart();renderLatestOrderButton();renderConnection()}
function renderConnection(){
  const b=$('connectionBanner');
  if(!window.BILIHAN_SUPABASE_CONFIGURED){b.textContent='Store database is not connected yet. Browsing demo/cache only; checkout is disabled.';b.classList.remove('hidden');return}
  if(!state.online){b.textContent='Ordering is temporarily unavailable. You can still browse our cached menu while we reconnect. ';const btn=document.createElement('button');btn.className='secondary-btn';btn.textContent='Try Again';btn.onclick=bootstrap;b.replaceChildren(document.createTextNode(b.textContent),btn);b.classList.remove('hidden')} else b.classList.add('hidden')
}
function renderSettings(){const s=state.data.settings||FALLBACK.settings;const logo=s.logo_url||'bilihan-logo.png';$('brandName').textContent=$('footerBrand').textContent=s.business_name;document.querySelectorAll('img[src="bilihan-logo.png"],img[data-store-logo]').forEach(img=>{img.src=logo;img.dataset.storeLogo='true'});$('heroTitle').textContent=s.hero_title;$('heroTagline').textContent=s.hero_tagline;$('aboutText').textContent=s.about_text;$('aboutImage').src=s.about_image_url||'bilihan-logo.png';$('footerPhone').textContent=s.phone;$('year').textContent=new Date().getFullYear();renderHero()}
function renderHero(){const imgs=state.data.settings?.hero_images||[];if(!imgs.length){$('heroImage').src='bilihan-logo.png';return}$('heroImage').src=imgs[state.heroIndex%imgs.length];$('heroDots').innerHTML=imgs.map((_,i)=>`<button class="${i===state.heroIndex?'active':''}" data-i="${i}" aria-label="Hero image ${i+1}"></button>`).join('');[...$('heroDots').children].forEach(b=>b.onclick=()=>{state.heroIndex=+b.dataset.i;renderHero()})}
setInterval(()=>{if(state.data&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const n=state.data.settings?.hero_images?.length||1;state.heroIndex=(state.heroIndex+1)%n;renderHero()}},5000);
function visibleCategories(){return (state.data.categories||[]).filter(c=>(state.data.products||[]).some(p=>p.category_id===c.id)).sort((a,b)=>a.sort_order-b.sort_order)}
function renderCategories(){const cats=visibleCategories();$('categoryTabs').innerHTML=[{id:'all',name:'All'},...cats].map(c=>`<button class="tab ${state.category===c.id?'active':''}" data-id="${c.id}">${esc(c.name)}</button>`).join('');[...$('categoryTabs').children].forEach(b=>b.onclick=()=>{state.category=b.dataset.id;renderCategories();renderProducts()})}
function renderProducts(){const ps=(state.data.products||[]).filter(p=>state.category==='all'||p.category_id===state.category).sort((a,b)=>a.sort_order-b.sort_order);const allSold=ps.length&&ps.every(p=>!p.is_available||p.stock<=0);$('menuGrid').innerHTML=(allSold?'<div class="status-banner" style="grid-column:1/-1">We’re currently sold out. Please check back again soon!</div>':'')+ps.map(p=>{const sold=!p.is_available||p.stock<=0;const stock=sold?'Sold Out':p.stock<=5?`Only ${p.stock} left!`:'Available';return `<article class="product-card" data-id="${p.id}"><img loading="lazy" src="${esc(p.image_url||'bilihan-logo.png')}" onerror="this.src='bilihan-logo.png'" alt="${esc(p.name)}"><div class="product-info"><div class="product-row"><strong>${esc(p.name)}</strong><span class="price">${money(p.price)}</span></div><div class="stock ${sold?'sold':''}">${stock}</div></div></article>`}).join('');document.querySelectorAll('.product-card').forEach(c=>c.onclick=()=>openProduct(c.dataset.id))}
function openProduct(id){const p=state.data.products.find(x=>x.id===id);const inCart=state.cart.find(x=>x.productId===id)?.qty||0;const max=Math.max(0,p.stock-inCart);const sold=!p.is_available||p.stock<=0;const cat=state.data.categories.find(c=>c.id===p.category_id)?.name||'';$('productDialog').innerHTML=`<div class="modal-body"><button class="icon-btn modal-close" onclick="productDialog.close()">✕</button><div class="product-modal-grid"><img src="${esc(p.image_url||'bilihan-logo.png')}" alt="${esc(p.name)}"><div><span class="eyebrow">${esc(cat)}</span><h2>${esc(p.name)}</h2><p>${esc(p.description)}</p><h3>${money(p.price)}</h3><p class="stock ${sold?'sold':''}">${sold?'Sold Out':p.stock<=5?`Only ${p.stock} left!`:'Available'}</p>${sold?'<button class="primary-btn" disabled>Sold Out</button>':max<=0?'<p class="muted">You already have the maximum available quantity in your cart.</p>':`<div class="qty"><button id="qMinus">−</button><strong id="qVal">1</strong><button id="qPlus">+</button></div><br><button id="addToCart" class="primary-btn">Add to Cart</button>`}</div></div></div>`;$('productDialog').showModal();let q=1;if($('qMinus')){$('qMinus').onclick=()=>{q=Math.max(1,q-1);$('qVal').textContent=q};$('qPlus').onclick=()=>{q=Math.min(max,q+1);$('qVal').textContent=q};$('addToCart').onclick=()=>{const ex=state.cart.find(x=>x.productId===id);if(ex)ex.qty+=q;else state.cart.push({productId:id,qty:q,price:+p.price,name:p.name,image:p.image_url});saveCart();$('productDialog').close();toast('Added to cart ✓')}}}
function renderCart(){
  $('cartCount').textContent=state.cart.reduce((s,i)=>s+i.qty,0);
  if(!state.cart.length){$('cartItems').innerHTML='<div style="text-align:center;padding:60px 20px"><h3>Your cart is empty</h3><button class="secondary-btn" id="browseBtn">Browse Menu</button></div>';$('cartFooter').innerHTML='';setTimeout(()=>$('browseBtn')&&($('browseBtn').onclick=closeCart),0);return}
  $('cartItems').innerHTML=state.cart.map((i,idx)=>`<div class="cart-item"><img src="${esc(i.image||'bilihan-logo.png')}" onerror="this.src='bilihan-logo.png'"><div class="cart-item-main"><strong>${esc(i.name)}</strong><span>${money(i.price)}</span><div class="cart-controls"><button class="qty-btn" data-a="minus" data-i="${idx}">−</button><span class="qty-value">${i.qty}</span><button class="qty-btn" data-a="plus" data-i="${idx}">+</button><button class="remove-btn" data-a="remove" data-i="${idx}">Remove</button></div></div></div>`).join('');
  const total=state.cart.reduce((s,i)=>s+i.qty*i.price,0);$('cartFooter').innerHTML=`<div class="summary-row"><strong>Total</strong><strong>${money(total)}</strong></div><button id="checkoutBtn" class="primary-btn" style="width:100%">Checkout</button>`;
  $('cartItems').querySelectorAll('button').forEach(b=>b.onclick=()=>cartAction(b.dataset.a,+b.dataset.i));$('checkoutBtn').onclick=openCheckout;
}
async function cartAction(a,i){const item=state.cart[i];if(a==='minus')item.qty=Math.max(1,item.qty-1);if(a==='plus'){const p=state.data.products.find(p=>p.id===item.productId);if(item.qty<(p?.stock||0))item.qty++;else toast('Maximum available stock reached')}if(a==='remove'&&confirm('Remove this item from your cart?'))state.cart.splice(i,1);saveCart()}
function openCart(){$('cartDrawer').classList.add('open');$('backdrop').classList.remove('hidden')}
function closeCart(){$('cartDrawer').classList.remove('open');$('backdrop').classList.add('hidden')}
$('cartBtn').onclick=openCart;$('closeCart').onclick=closeCart;$('backdrop').onclick=closeCart;
function validateCartAgainstLive(liveProducts){let changed=false, invalid=[];for(const item of state.cart){const p=liveProducts.find(x=>x.id===item.productId);if(!p){invalid.push(`${item.name} is no longer available.`);changed=true;continue}if(!p.is_available||p.stock<item.qty){invalid.push(`${item.name} no longer has enough stock.`);changed=true}if(+p.price!==+item.price){item.price=+p.price;invalid.push(`${item.name} price was updated.`);changed=true}}if(changed)saveCart();return invalid}
async function fetchLiveProducts(){const {data,error}=await db.from('products').select('*');if(error)throw error;return data}
async function syncOrderToGoogleSheet(order){try{if(!GOOGLE_SHEETS_WEB_APP_URL)return;const items=(order.items||[]).map(i=>`${i.product_name} x ${i.qty}`).join(', ');const payload={order_id:order.id||order.order_id||order.order_code,order_code:order.order_code||'',order_date:order.created_at||new Date().toISOString(),customer_name:order.customer_name||'',phone:order.phone||'',fulfillment:order.fulfillment||'',address:order.address||'',preferred_date:order.preferred_date||'',payment_method:order.payment_method||'',items:items,subtotal:Number(order.subtotal??order.total??0),delivery_fee:Number(order.delivery_fee||0),total:Number(order.total||0),payment_status:order.payment_status||'Pending',order_status:order.status||'Pending',cancellation_reason:order.cancellation_reason||''};await fetch(GOOGLE_SHEETS_WEB_APP_URL,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(payload)})}catch(err){console.warn('Google Sheets sync failed:',err)}}
function fileToBase64(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result).split(',')[1]||'');reader.onerror=()=>reject(new Error('Unable to read receipt file.'));reader.readAsDataURL(file)})}
function formatFileSize(bytes){if(bytes<1024)return `${bytes} B`;if(bytes<1024*1024)return `${(bytes/1024).toFixed(0)} KB`;return `${(bytes/1024/1024).toFixed(1)} MB`}
async function compressReceiptImage(file){const allowed=['image/jpeg','image/png','image/webp','application/pdf'];if(!allowed.includes(file.type))throw new Error('Receipt must be JPG, PNG, WEBP, or PDF.');if(file.type==='application/pdf'){if(file.size>3*1024*1024)throw new Error('PDF receipt must be 3 MB or smaller.');return file}if(file.size>12*1024*1024)throw new Error('Receipt image is too large. Please use an image smaller than 12 MB.');const bitmap=await createImageBitmap(file);const maxSide=1400;const scale=Math.min(1,maxSide/Math.max(bitmap.width,bitmap.height));const width=Math.max(1,Math.round(bitmap.width*scale));const height=Math.max(1,Math.round(bitmap.height*scale));const canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const ctx=canvas.getContext('2d',{alpha:false});ctx.drawImage(bitmap,0,0,width,height);bitmap.close?.();const blob=await new Promise((resolve,reject)=>canvas.toBlob(b=>b?resolve(b):reject(new Error('Unable to optimize receipt image.')),'image/jpeg',0.72));const baseName=(file.name||'receipt').replace(/\.[^.]+$/,'');return new File([blob],`${baseName}.jpg`,{type:'image/jpeg',lastModified:Date.now()})}
async function uploadReceiptToGoogleDrive(order,file){if(!order?.order_code)throw new Error('Order number is missing.');if(!file)throw new Error('Please upload your payment receipt.');const file_base64=await fileToBase64(file);const payload={action:'upload_receipt',order_code:order.order_code,file_name:file.name,mime_type:file.type,file_base64};const response=await fetch(GOOGLE_SHEETS_WEB_APP_URL,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(payload),cache:'no-store'});return response}
function openCheckout(){closeCart();if(!window.BILIHAN_SUPABASE_CONFIGURED||!state.online)return alert('Ordering is temporarily unavailable until the database connection is restored.');const s=state.data.settings;$('checkoutDialog').innerHTML=`<div class="modal-body"><button class="icon-btn modal-close" onclick="checkoutDialog.close()">✕</button><span class="eyebrow">Guest checkout</span><h2>Complete your order</h2><form id="checkoutForm" class="form-grid"><label class="field">Name *<input name="name" required></label><label class="field">Mobile number (optional)<input name="phone"></label><label class="field">Receive order *<select name="fulfillment"><option>Delivery</option><option>Pickup</option></select></label><label class="field">Preferred date *<input name="preferredDate" type="date" required min="${new Date().toISOString().slice(0,10)}"></label><label id="addressField" class="field full">Delivery address *<textarea name="address"></textarea></label><div id="pickupInfo" class="field full hidden"><div class="status-banner"><strong>Pickup location:</strong> ${esc(s.pickup_location||'')}</div></div><label class="field">Payment *<select name="payment"><option>QR Payment</option><option>Cash on Delivery / Pickup</option></select></label><label class="field full">Customer note<textarea name="note"></textarea></label><div id="paymentInfo" class="field full"></div><div class="field full summary" id="checkoutSummary"></div><label class="field full"><span><input type="checkbox" name="confirm" id="confirmOrder" required> I confirm that my order and contact details are correct.</span></label><div class="field full"><button class="primary-btn" id="placeOrderBtn" disabled>Place Order</button><p class="muted" id="orderProgress" style="margin-top:8px"></p></div></form></div>`;$('checkoutDialog').showModal();const f=$('checkoutForm');f._receiptPrepared=null;f._receiptPreparing=false;const updatePlaceOrderButton=()=>{const qr=f.payment.value==='QR Payment';const hasReceipt=!qr||!!f._receiptPrepared;const confirmed=$('confirmOrder')?.checked;$('placeOrderBtn').disabled=!!f._receiptPreparing||!(hasReceipt&&confirmed)};const renderDynamic=()=>{const pickup=f.fulfillment.value==='Pickup';$('addressField').classList.toggle('hidden',pickup);$('pickupInfo').classList.toggle('hidden',!pickup);f.address.required=!pickup;f._receiptPrepared=null;f._receiptPreparing=false;if(f.payment.value==='QR Payment'){const qrUrl=s.qr_image_url||'bilihan-logo.png';$('paymentInfo').innerHTML=`<div class="summary"><img id="paymentQrImage" src="${esc(qrUrl)}" style="max-width:220px;border-radius:16px"><p>Scan or save the QR code to complete your payment.</p><a class="secondary-btn" href="${esc(qrUrl)}" download="Bilihan-QR-Code" target="_blank">Save QR Code</a><div style="margin-top:18px"><label class="field"><strong>Upload payment receipt *</strong><input name="receipt" id="paymentReceipt" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required></label><p class="muted" id="receiptStatus">No receipt selected yet.</p></div></div>`;const receipt=$('paymentReceipt');receipt.onchange=async()=>{const file=receipt.files[0];f._receiptPrepared=null;if(!file){$('receiptStatus').textContent='No receipt selected yet.';updatePlaceOrderButton();return}f._receiptPreparing=true;$('receiptStatus').textContent='Optimizing receipt…';updatePlaceOrderButton();try{const prepared=await compressReceiptImage(file);f._receiptPrepared=prepared;$('receiptStatus').textContent=`Receipt ready: ${prepared.name} (${formatFileSize(prepared.size)})`}catch(err){receipt.value='';f._receiptPrepared=null;$('receiptStatus').textContent=err.message;alert(err.message)}finally{f._receiptPreparing=false;updatePlaceOrderButton()}}}else{$('paymentInfo').innerHTML='<div class="status-banner">Please prepare the exact amount when possible.</div>'}updatePlaceOrderButton();$('checkoutSummary').innerHTML=state.cart.map(i=>`<div class="summary-row"><span>${esc(i.name)} × ${i.qty}</span><strong>${money(i.price*i.qty)}</strong></div>`).join('')+`<hr><div class="summary-row"><strong>Total</strong><strong>${money(state.cart.reduce((s,i)=>s+i.qty*i.price,0))}</strong></div>`};f.fulfillment.onchange=renderDynamic;f.payment.onchange=renderDynamic;$('confirmOrder').onchange=updatePlaceOrderButton;renderDynamic();f.onsubmit=placeOrder}
async function placeOrder(e){e.preventDefault();const f=e.currentTarget;const btn=$('placeOrderBtn');const progress=$('orderProgress');const isQr=f.payment.value==='QR Payment';const receiptFile=isQr?f._receiptPrepared:null;if(isQr&&!receiptFile){alert('Please upload your payment receipt before placing the order.');return}btn.disabled=true;btn.textContent='Creating order…';if(progress)progress.textContent='Creating order…';try{const live=await fetchLiveProducts();const invalid=validateCartAgainstLive(live);if(invalid.length){alert('Please review your cart before ordering:\n\n'+invalid.join('\n'));$('checkoutDialog').close();openCart();return}const d=Object.fromEntries(new FormData(f));const items=state.cart.map(i=>({product_id:i.productId,qty:i.qty}));const {data,error}=await db.rpc('place_order',{p_customer_name:d.name,p_phone:d.phone?d.phone.trim()||null:null,p_fulfillment:d.fulfillment,p_address:d.fulfillment==='Delivery'?d.address:null,p_preferred_date:d.preferredDate,p_payment_method:d.payment,p_note:d.note||null,p_items:items});if(error)throw error;if(!data?.ok)throw new Error(data?.error||'Order could not be placed');const order=data.order;if(isQr&&receiptFile){btn.textContent='Uploading receipt…';if(progress)progress.textContent=`Uploading receipt for Order #${order.order_code}…`;await uploadReceiptToGoogleDrive(order,receiptFile)}btn.textContent='Finalizing…';if(progress)progress.textContent='Finalizing order…';syncOrderToGoogleSheet(order);localStorage.setItem(LS.latestOrder,JSON.stringify(order));state.cart=[];saveCart();$('checkoutDialog').close();await bootstrap();showOrder(order)}catch(err){console.error(err);alert((err.message||'Unable to place order. Please try again.')+' If an order number was already created, check Supabase/Admin before trying again.')}finally{btn.disabled=false;btn.textContent='Place Order';if(progress)progress.textContent=''}}
function showOrder(order){const cancelled=order.status==='Cancelled';$('orderDialog').innerHTML=`<div class="modal-body"><button class="icon-btn modal-close" onclick="orderDialog.close()">✕</button><span class="eyebrow">${cancelled?'Order cancelled':'Order confirmed'}</span><h2>#${esc(order.order_code)}</h2><p>${new Date(order.created_at).toLocaleString()}</p><div class="summary">${(order.items||[]).map(i=>`<div class="summary-row"><span>${esc(i.product_name)} × ${i.qty}</span><strong>${money(i.unit_price*i.qty)}</strong></div>`).join('')}<hr><div class="summary-row"><strong>Total</strong><strong>${money(order.total)}</strong></div><p>${esc(order.fulfillment)} · ${esc(order.preferred_date)} · ${esc(order.payment_method)}</p></div><div class="contact-actions" style="margin-top:16px"><button class="secondary-btn" id="copyOrderNo">Copy Order Number</button>${!cancelled?'<button class="danger-btn" id="cancelOrderBtn">Cancel Order</button>':'<button class="secondary-btn" disabled>Order Cancelled</button>'}<button class="primary-btn" id="continueBtn">Continue Shopping</button></div><div id="cancelState"></div></div>`;$('orderDialog').showModal();$('copyOrderNo').onclick=()=>navigator.clipboard.writeText(order.order_code).then(()=>toast('Copied ✓'));$('continueBtn').onclick=()=>$('orderDialog').close();if($('cancelOrderBtn')){const deadline=new Date(order.created_at).getTime()+3*60*60*1000;if(Date.now()>deadline)$('cancelOrderBtn').disabled=true;else $('cancelOrderBtn').onclick=()=>cancelOrder(order)}}
function orderText(o){return `Bilihan Order #${o.order_code}\n${(o.items||[]).map(i=>`${i.product_name} x ${i.qty}`).join('\n')}\nTotal: ${money(o.total)}\n${o.fulfillment} — ${o.preferred_date}\n${o.payment_method}`}
async function cancelOrder(order){const reason=prompt('Why are you cancelling this order?');if(!reason||!reason.trim())return;try{const {data,error}=await db.rpc('cancel_order',{p_order_code:order.order_code,p_cancel_token:order.cancel_token,p_reason:reason.trim(),p_requested_at:new Date().toISOString()});if(error)throw error;if(!data?.ok)throw new Error(data?.error||'Unable to cancel');const updated={...order,status:'Cancelled',cancellation_reason:reason.trim(),cancelled_at:new Date().toISOString()};localStorage.setItem(LS.latestOrder,JSON.stringify(updated));await bootstrap();showOrder(updated);if(order.payment_method==='QR Payment')alert('Order cancelled. If you have already sent your payment, please contact us through Messenger or Instagram regarding your refund.')}catch(e){const pending={order,reason:reason.trim(),requestedAt:new Date().toISOString()};localStorage.setItem(LS.pendingCancel,JSON.stringify(pending));$('cancelState').innerHTML='<div class="status-banner">Cancellation Pending</div>';toast('Cancellation queued')}}
async function retryPendingCancel(){if(!window.BILIHAN_SUPABASE_CONFIGURED)return;const raw=localStorage.getItem(LS.pendingCancel);if(!raw)return;const p=JSON.parse(raw);try{const {data,error}=await db.rpc('cancel_order',{p_order_code:p.order.order_code,p_cancel_token:p.order.cancel_token,p_reason:p.reason,p_requested_at:p.requestedAt});if(error)throw error;if(data?.ok){localStorage.removeItem(LS.pendingCancel);const updated={...p.order,status:'Cancelled',cancellation_reason:p.reason};localStorage.setItem(LS.latestOrder,JSON.stringify(updated))}}catch(e){console.warn('Pending cancellation still waiting',e)}}
function renderLatestOrderButton(){const o=localStorage.getItem(LS.latestOrder);$('myOrderBtn').classList.toggle('hidden',!o);$('myOrderBtn').onclick=()=>o&&showOrder(JSON.parse(localStorage.getItem(LS.latestOrder)))}
$('themeToggle').onclick=()=>{const dark=document.documentElement.dataset.theme==='dark';document.documentElement.dataset.theme=dark?'light':'dark';localStorage.setItem(LS.theme,dark?'light':'dark');$('themeToggle').textContent=dark?'🌙':'☀️'};document.documentElement.dataset.theme=localStorage.getItem(LS.theme)||'light';$('themeToggle').textContent=document.documentElement.dataset.theme==='dark'?'☀️':'🌙';
bootstrap();
